server:
  port: 8080

spring:
  datasource:
    username: root
    password: 082021MSVGreen!
    url: jdbc:h2:mem:search_engine
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    open-in-view: true

  h2:
    console:
      enabled:true

  sql:
    init:
      data-locations:data-test.sql

